*)This folder contains coding files for our reserch project.

*)For the save side,There are html saved Juypyter Notebook folder which contains 5 jupyter notebook files in html format which is uneditable.

*) As the project is built on python language by using jupyter notebook as an Ide on Anaconda virtual environment so we need install packages
 on anaconda's terminal.The most of the libraries are taken Surprise package which we is an official python recommendation library.

So it is important to install all initial libraries in condas virtual terminal.

Installation of surprise library package needs to use following code in condas virtual terminal as an Adminstrator.

"conda install -c conda-forge scikit-surprise"

I takes time to download.

*) Folder 'ml-latest-small' contains our dataset files i.e. movies.csv and rating.csv which are utilized in our coding